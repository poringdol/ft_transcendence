<%= form_with(model: tournament_match, local: true) do |form| %>
  <% if tournament_match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tournament_match.errors.count, "error") %> prohibited this tournament_match from being saved:</h2>

      <ul>
        <% tournament_match.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tournament_id %>
    <%= form.text_field :tournament_id %>
  </div>

  <div class="field">
    <%= form.label :match_id %>
    <%= form.text_field :match_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
